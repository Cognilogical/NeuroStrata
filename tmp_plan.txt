I'm working on the MindBank feature integration. Currently evaluating the steps:

1. **Update LanceDB Schema (`src/store/lancedb.rs`)**:
   Add `related_to` (String array or JSON list), `valid_from` (i64/timestamp), and `valid_to` (i64/timestamp). Note: Arrow schema modification in LanceDB can be tricky for existing tables, so I might need to write a migration or assume a fresh db format (since it's a new system).

2. **Temporal Versioning (`upsert` in `src/store/lancedb.rs`)**:
   Updating existing records to soft-delete (`valid_to = now()`) is complex if LanceDB doesn't easily support targeted row updates without replacing the batch. I'll need to figure out LanceDB's `.update()` functionality or just append and rely on `valid_to IS NULL` filters during search.

3. **Hybrid Search (LanceDB Tantivy integration)**:
   In `lancedb=0.27.2`, creating an FTS index allows hybrid search. Need to add `.create_index("content").index_type(IndexType::FTS).execute()` to the table initialization.

Let's look deeper into how `upsert` and `init` are structured in `src/store/lancedb.rs`.
