name: "Deploy v1.0.0 (Nightly)"

on:
  push:
    branches:
      - main

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build NeuroStrata Unified Core + Shims
        run: |
          chmod +x ./mcp/build.sh
          
          # Install dependencies for the typescript plugin
          cd plugins/opencode/neurostrata-plugin && npm ci && cd ../..
          
          # Execute the build script
          ./mcp/build.sh
          
          # Ensure consistent asset names
          if [ -f dist/opencode-neurostrata-1.0.0.tgz ]; then
            mv dist/opencode-neurostrata-1.0.0.tgz dist/opencode-neurostrata.tgz
          fi

      - name: Clobber v1.0.0 Release Assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload v1.0.0 \
            bin/neurostrata-mcp-linux-amd64 \
            bin/neurostrata-mcp-linux-arm64 \
            bin/neurostrata-mcp-darwin-amd64 \
            bin/neurostrata-mcp-darwin-arm64 \
            bin/neurostrata-mcp-windows-amd64.exe \
            dist/opencode-neurostrata.tgz \
            --clobber
